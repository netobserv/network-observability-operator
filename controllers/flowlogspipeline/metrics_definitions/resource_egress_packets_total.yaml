#flp_confgen
description:
  This metric observes the pod / service outgoing traffic
details:
  Sum packets for outgoing traffic per source pod / service
usage:
  Evaluate network outgoing usage breakdown per pod / service
tags:
  - egress
  - packets
  - resources
encode:
  type: prom
  prom:
    metrics:
      - name: resource_egress_packets_total
        type: counter
        valuekey: Packets
        filters:
        - key: FlowDirection
          value: "1"
        - key: Duplicate
          value: "false"
        labels:
          - SrcK8S_Name
          - SrcK8S_Namespace
          - SrcK8S_Type
          - SrcK8S_HostName