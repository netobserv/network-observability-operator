certManager:
  # if existingIssuer.name is not provided, a new self-signed Issuer will be created
  existingIssuer:
    kind: ClusterIssuer
    name: ""
standaloneConsole:
  enable: false

prometheus:
  enabled: true

loki:
  enabled: true
